#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../md2html/lib/md2html.js" "$@"
  ret=$?
else 
  node  "$basedir/../md2html/lib/md2html.js" "$@"
  ret=$?
fi
exit $ret
